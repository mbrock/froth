<?xml version="1.0" encoding="UTF-8"?>
<?xml-stylesheet type="text/xsl" href="/rfc/rfc.xsl"?>
<rfc number="0001" xmlns="https://froth.1.foo/rfc">
  <meta>
    <title>FROTH-RFC-0001: In-Browser Video Encoding via WebCodecs</title>
    <status>OBSOLETE</status>
    <author>Charlie (@charliebuddybot)</author>
    <date>2026-03-20</date>
    <supersedes>The screenshot-to-PNG-to-ffmpeg pipeline</supersedes>
  </meta>
  <section title="Problem" id="problem">
    <p>Rendering a 4-minute video at 24fps currently requires:</p>
    <list type="numbered">
      <item><p>6083 calls to Browser.screenshot()</p></item>
      <item><p>6083 PNG files written to disk (9.7GB)</p></item>
      <item><p>One ffmpeg invocation reading all PNGs back</p></item>
      <item><p>15 minutes wall time (serial) or 10 minutes (4 workers)</p></item>
    </list>
    <p>The browser renders every pixel. Then we photograph the pixels. Then we give the photographs to a C program that turns them back into pixels in a different format. The intermediate representation is nine gigabytes of lossless bitmaps on a filesystem.</p>
  </section>
  <section title="Proposal" id="proposal">
    <p>Replace the screenshot+ffmpeg pipeline with WebCodecs running inside the same Chrome instance that renders the frames.</p>
    <p>The browser renders the frame. The browser encodes the frame. The browser muxes the encoded frame. No screenshot. No PNG. No disk. No ffmpeg. The pixels never leave the GPU.</p>
  </section>
  <section title="Architecture" id="architecture">
    <subsection title="Encoder Script (injected into the reel HTML)" id="encoder-script-injected-into-the-reel-html">
      <code>async function encodeVideo(durationS, fps) {
  const canvas = document.querySelector('#video canvas')
    || createOffscreenCanvas();

  const chunks = [];
  const encoder = new VideoEncoder({
    output: (chunk, meta) =&gt; {
      const buf = new ArrayBuffer(chunk.byteLength);
      chunk.copyTo(buf);
      chunks.push({
        type: chunk.type,
        timestamp: chunk.timestamp,
        duration: chunk.duration,
        data: btoa(String.fromCharCode(...new Uint8Array(buf)))
      });
    },
    error: (e) =&gt; console.error('Encoder error:', e)
  });

  encoder.configure({
    codec: 'avc1.640028',       // H.264 High Profile Level 4.0
    width: 1080,
    height: 1920,
    bitrate: 4_000_000,         // 4 Mbps
    framerate: fps,
    latencyMode: 'quality',
    avc: { format: 'annexb' }   // Raw NAL units
  });

  const frameCount = Math.ceil(durationS <em> fps);

  for (let i = 0; i &lt; frameCount; i++) {
    const t = i / fps;
    window.FrothVideo.renderAt(t);

    // Capture the current viewport as a VideoFrame
    const frame = new VideoFrame(canvas, {
      timestamp: Math.round(t </em> 1_000_000),  // microseconds
      duration: Math.round(1_000_000 / fps)
    });

    const keyFrame = (i % (fps * 2)) === 0;  // keyframe every 2s
    encoder.encode(frame, { keyFrame });
    frame.close();

    // Yield to prevent UI lockup (optional in headless)
    if (i % 100 === 0) await new Promise(r =&gt; setTimeout(r, 0));
  }

  await encoder.flush();
  encoder.close();

  return chunks;
}</code>
    </subsection>
    <subsection title="Canvas Requirement" id="canvas-requirement">
      <p>WebCodecs needs a canvas or offscreen canvas to create VideoFrames. The current template renders to DOM elements (divs with background images, text spans). Two options:</p>
      <p>A. <strong>html2canvas approach</strong>: After each renderAt(), use    html2canvas or a similar library to rasterize the DOM to a    canvas. Adds ~20ms per frame. Total overhead: ~2 minutes    for a 4-minute video.</p>
      <p>B. <strong>Native canvas rendering</strong>: Rewrite the renderer to paint    directly to a canvas using CanvasRenderingContext2D. Faster    but loses CSS typography, transitions, and layout. Wrong    tradeoff for our use case where the CSS IS the production    value.</p>
      <p>C. <strong>OffscreenCanvas from screenshot</strong>: Use Chrome DevTools    Page.captureScreenshot but decode it into a VideoFrame    instead of writing to disk. Hybrid approach: DOM rendering    for quality, WebCodecs for encoding. Still uses DevTools    but eliminates disk I/O.</p>
      <p>D. <strong>Chrome's --enable-features=CanvasOopRasterization</strong>: With    GPU rasterization enabled in headless Chrome, the entire    page render is already on the GPU. A VideoFrame can be    constructed from the compositor output via    <tt>document.startViewTransition</tt> or similar emerging APIs.    This is the zero-copy path. Not stable yet.</p>
      <p><strong>Recommended: Option C for v1, Option D when Chrome ships it.</strong></p>
    </subsection>
    <subsection title="Muxing" id="muxing">
      <p>Encoded chunks need to be assembled into an MP4 container.</p>
      <p><strong>In-browser (preferred):</strong></p>
      <list type="bullet">
        <item><p>mp4-muxer (npm): ~50KB, produces spec-compliant fMP4</p></item>
        <item><p>Inject via CDN URL or inline the library</p></item>
      </list>
      <p><strong>Server-side fallback:</strong></p>
      <list type="bullet">
        <item><p>Stream chunks back to Elixir via DevTools eval</p></item>
        <item><p>Assemble with an Elixir MP4 writer or pipe to ffmpeg</p></item>
        <item><p>Still eliminates PNGs; ffmpeg reads raw H.264 from stdin</p></item>
      </list>
    </subsection>
    <subsection title="Audio" id="audio">
      <p>WebCodecs has AudioEncoder. The podcast MP3 can be decoded via AudioContext.decodeAudioData(), re-encoded as AAC via AudioEncoder, and muxed alongside the video track. Or: mux the video-only MP4 server-side and add the audio with one ffmpeg call (ffmpeg -i video.mp4 -i audio.mp3 -c copy out.mp4). The audio mux is trivial. Do not let it block the video path.</p>
    </subsection>
  </section>
  <section title="Integration with Froth.Compute" id="integration-with-froth-compute">
    <p>The distributed compute system (RFC pending) remains valuable. Each compute worker runs Chrome with the encoder inside it. Instead of:</p>
    <code>worker claims task → boots Chrome → screenshots N frames
→ writes PNGs → completes task with file artifacts
</code>
    <p>It becomes:</p>
    <code>worker claims task → boots Chrome → encodes N frames
→ returns binary H.264 segment → completes task with
  binary artifact
</code>
    <p>The coordination layer (jobs, tasks, leases, heartbeats) is unchanged. The artifact kind changes from &quot;frame_batch_png&quot; to &quot;encoded_segment_h264&quot;. The final mux concatenates segments instead of encoding from PNGs.</p>
    <p>Parallel encoding across nodes: each node encodes its time range independently. Keyframe alignment at segment boundaries ensures clean concatenation. The ffmpeg concat demuxer handles this natively:</p>
    <code>ffmpeg -f concat -i segments.txt -c copy final.mp4
</code>
  </section>
  <section title="Performance Estimate" id="performance-estimate">
    <p>Current pipeline (screenshot + ffmpeg):</p>
    <list type="bullet">
      <item><p>55ms per frame capture (screenshot to PNG)</p></item>
      <item><p>6083 frames × 55ms = 335s (5.5 min serial)</p></item>
      <item><p>4 workers: ~165s (2.75 min)</p></item>
      <item><p>Plus ffmpeg mux: ~60s</p></item>
      <item><p>Total: ~4 min with 4 workers</p></item>
    </list>
    <p>WebCodecs pipeline (Option C):</p>
    <list type="bullet">
      <item><p>55ms per frame capture (screenshot via DevTools)</p></item>
      <item><p>~2ms per frame encode (hardware H.264)</p></item>
      <item><p>6083 frames × 57ms = 347s serial</p></item>
      <item><p>4 workers: ~87s (1.5 min)</p></item>
      <item><p>Plus segment concat: ~5s</p></item>
      <item><p>Total: ~1.5 min with 4 workers</p></item>
      <item><p>Disk I/O: zero intermediate files</p></item>
    </list>
    <p>WebCodecs pipeline (Option D, future):</p>
    <list type="bullet">
      <item><p>~15ms per frame (DOM render only, no screenshot)</p></item>
      <item><p>~2ms per frame encode</p></item>
      <item><p>6083 frames × 17ms = 103s serial</p></item>
      <item><p>4 workers: ~26s</p></item>
      <item><p>Total: ~30s with 4 workers</p></item>
    </list>
  </section>
  <section title="Implementation Status" id="implementation-status">
    <p>This RFC is obsolete as of 2026-04-16.</p>
    <p><tt>Froth.Video.SelfEncoder</tt> was implemented as a prototype, but the Video, Compute, and Browser subsystems were later removed in commit <tt>c8d8803</tt> because they were unused. No WebCodecs render path remains in the current repository.</p>
    <p>The document is retained as historical design context; it does not describe a currently available or partially implemented Froth feature.</p>
  </section>
  <section title="Migration Path" id="migration-path">
    <list type="numbered">
      <item><p>Keep current screenshot pipeline as fallback</p></item>
      <item><p>Implement Option C: DevTools screenshot → VideoFrame → encode</p></item>
      <item><p>Stream encoded chunks back via DevTools Runtime.evaluate</p></item>
      <item><p>Wire into ComputeRenderer as the default encode path</p></item>
      <item><p>Measure. If faster, delete the PNG path.</p></item>
      <item><p>When Chrome ships zero-copy compositor access, implement Option D</p></item>
    </list>
  </section>
  <section title="Open Questions" id="open-questions">
    <list type="bullet">
      <item><p>Does headless Chrome support hardware-accelerated VideoEncoder? If not, software H.264 encoding adds ~10ms/frame. Still faster than writing PNGs to disk.</p></item>
      <item><p>Maximum chunk size returnablvia DevTools eval? May need to stream chunks via WebSocket instead of returning them all at once. The ComputeWorkerChannel already speaks WebSocket.</p></item>
      <item><p>Can OffscreenCanvas capture the full page render including CSS animations, or only explicit canvas draws? If the latter, Option C (DevTools screenshot → VideoFrame) is the correct v1.</p></item>
    </list>
  </section>

</rfc>
