<?xml version="1.0" encoding="UTF-8"?>
<?xml-stylesheet type="text/xsl" href="/rfc/rfc.xsl"?>
<rfc number="0004" xmlns="https://froth.1.foo/rfc">
  <meta>
    <title>FROTH-RFC-0004: Agent Execution Spine</title>
    <status>PARTIALLY SUPERSEDED</status>
    <author>Charlie (@charliebuddybot)</author>
    <date>2026-03-23</date>
    <related>FROTH-RFC-0002, FROTH-RFC-0003</related>
  </meta>
  <section title="Situation" id="situation">
    <p>RFC-0002 pushes Froth toward a native multimodal LLM layer. RFC-0003 fixes a concrete agent-runtime bug around parallel tool execution. Both are necessary. Neither is the next bottleneck.</p>
    <p>The next bottleneck is that execution state is split across four different worlds:</p>
    <list type="bullet">
      <item><p>in-memory process state inside <tt>Froth.Agent.Worker</tt></p></item>
      <item><p>semantic transcript state in <tt>agent_messages</tt></p></item>
      <item><p>minimal sequencing state in <tt>agent_events</tt></p></item>
      <item><p>low-level span data in <tt>telemetry_events</tt></p></item>
    </list>
    <p>That split was fine while the agent subsystem was small. It is starting to hurt now.</p>
    <p><tt>agent_cycles</tt> currently stores almost nothing except an ID and timestamps. <tt>agent_events</tt> is only a head pointer plus a sequence number. <tt>agent_messages</tt> stores the semantic content and some metadata, but not the runtime structure of the cycle. Telemetry has the opposite problem: rich runtime detail exists, but it is not the primary data model of the agent subsystem. It is just a span stream.</p>
    <p>This means Froth can reconstruct a transcript, but not a run.</p>
    <p>Questions that should be first-class are currently awkward:</p>
    <list type="bullet">
      <item><p>What exact resolved config did this cycle run with?</p></item>
      <item><p>Which LLM calls happened, in what order, with what latency and usage?</p></item>
      <item><p>Which tool calls started, succeeded, failed, or timed out?</p></item>
      <item><p>What blob or media payloads were involved, and where are they stored?</p></item>
      <item><p>Which telemetry spans correspond to which cycle turns?</p></item>
      <item><p>Can a cycle be resumed, retried, or inspected without scraping the transcript?</p></item>
    </list>
    <p>As multimodal support grows, this gets worse. If media blobs and raw vendor payloads live inline in JSONB transcript rows, storage gets hot, indexes get worse, and the database becomes the accidental blob store.</p>
    <p>At the same time, the Telegram-facing side of the system is already moving toward normalized message history plus linked cycle traces. That is the right direction. The persistence model should now catch up to the runtime model.</p>
  </section>
  <section title="Goal" id="goal">
    <p>Build a durable execution spine for Froth's LLM and agent runtime:</p>
    <list type="bullet">
      <item><p>semantic messages remain available as the transcript</p></item>
      <item><p>runs become first-class records, not inferred after the fact</p></item>
      <item><p>tool calls and LLM calls become queryable operational units</p></item>
      <item><p>telemetry spans attach cleanly to persisted execution records</p></item>
      <item><p>large payloads and media move out of hot JSONB rows</p></item>
      <item><p>app-specific linkage such as Telegram remains possible without defining the core runtime model</p></item>
    </list>
  </section>
  <section title="Non-Goals" id="non-goals">
    <list type="bullet">
      <item><p>This RFC does not replace Postgres with a data lake.</p></item>
      <item><p>This RFC does not redesign prompt construction.</p></item>
      <item><p>This RFC does not require every streamed token delta to be persisted.</p></item>
      <item><p>This RFC does not remove provider-specific traces or telemetry.</p></item>
      <item><p>This RFC is not a proposal for distributed agent scheduling yet.</p></item>
    </list>
  </section>
  <section title="Thesis" id="thesis">
    <p>The next layer after native provider adapters is not another wrapper. It is an execution spine.</p>
    <p>Froth should treat &quot;agent cycle&quot;, &quot;LLM call&quot;, &quot;tool invocation&quot;, &quot;semantic message&quot;, &quot;blob/media artifact&quot;, and &quot;telemetry span&quot; as connected pieces of one run. Today those pieces exist, but they do not line up in one durable model.</p>
    <p>The right move is:</p>
    <list type="numbered">
      <item><p>make <tt>agent_cycles</tt> a real run record</p></item>
      <item><p>turn <tt>agent_events</tt> into a durable execution log</p></item>
      <item><p>keep <tt>agent_messages</tt> as the semantic transcript</p></item>
      <item><p>move large blobs and raw payloads out of transcript tables</p></item>
      <item><p>persist enough config and provenance to make runs inspectable and resumable</p></item>
    </list>
  </section>
  <section title="Proposed Changes" id="proposed-changes">
    <subsection title="1. Promote `agent_cycles` from ID holder to run record" id="1-promote-agent-cycles-from-id-holder-to-run-record">
      <p><tt>agent_cycles</tt> should become the durable summary of a run.</p>
      <p>It should carry fields like:</p>
      <list type="bullet">
        <item><p><tt>status</tt> (<tt>queued</tt>, <tt>running</tt>, <tt>waiting_on_tools</tt>, <tt>completed</tt>, <tt>failed</tt>, <tt>cancelled</tt>)</p></item>
        <item><p>resolved <tt>provider</tt></p></item>
        <item><p>resolved <tt>model</tt></p></item>
        <item><p><tt>root_span_id</tt></p></item>
        <item><p><tt>parent_span_id</tt></p></item>
        <item><p><tt>config_json</tt></p></item>
        <item><p><tt>system_prompt_ref</tt> or <tt>system_prompt_hash</tt></p></item>
        <item><p><tt>toolset_hash</tt></p></item>
        <item><p>aggregate <tt>usage</tt></p></item>
        <item><p>aggregate <tt>cost_usd</tt></p></item>
        <item><p><tt>error</tt></p></item>
        <item><p><tt>started_at</tt></p></item>
        <item><p><tt>finished_at</tt></p></item>
      </list>
      <p>The key point is that a cycle should be inspectable without replaying the whole transcript and without scraping telemetry to recover basic facts about what happened.</p>
    </subsection>
    <subsection title="2. Turn `agent_events` into the execution log" id="2-turn-agent-events-into-the-execution-log">
      <p><tt>agent_events</tt> should stop being only &quot;head moved to message X&quot;. That information is useful, but it is not enough.</p>
      <p>Instead, <tt>agent_events</tt> should be an append-only log of meaningful runtime transitions. Example kinds:</p>
      <list type="bullet">
        <item><p><tt>cycle.started</tt></p></item>
        <item><p><tt>llm.requested</tt></p></item>
        <item><p><tt>llm.completed</tt></p></item>
        <item><p><tt>tool.started</tt></p></item>
        <item><p><tt>tool.completed</tt></p></item>
        <item><p><tt>tool.failed</tt></p></item>
        <item><p><tt>message.appended</tt></p></item>
        <item><p><tt>cycle.completed</tt></p></item>
        <item><p><tt>cycle.failed</tt></p></item>
      </list>
      <p>Each event row should be able to carry:</p>
      <list type="bullet">
        <item><p><tt>cycle_id</tt></p></item>
        <item><p><tt>seq</tt></p></item>
        <item><p><tt>kind</tt></p></item>
        <item><p><tt>span_id</tt></p></item>
        <item><p><tt>parent_span_id</tt></p></item>
        <item><p><tt>message_id</tt></p></item>
        <item><p><tt>tool_use_id</tt></p></item>
        <item><p><tt>data_json</tt></p></item>
        <item><p><tt>blob_ref</tt></p></item>
      </list>
      <p>This does not mean persisting every token delta. The log should store turn and operation boundaries, not an unbounded firehose of tiny events. Live token streaming can remain PubSub + telemetry.</p>
    </subsection>
    <subsection title="3. Keep `agent_messages` as the semantic transcript" id="3-keep-agent-messages-as-the-semantic-transcript">
      <p><tt>agent_messages</tt> is still the right place for the semantic content chain:</p>
      <list type="bullet">
        <item><p>user messages</p></item>
        <item><p>assistant messages</p></item>
        <item><p>tool results</p></item>
        <item><p>multimodal content blocks</p></item>
      </list>
      <p>But it should stop carrying the burden of also being the runtime execution log. It is the transcript, not the scheduler, not the trace store, and not the blob store.</p>
      <p>This also keeps the message model aligned with RFC-0002: providers produce semantic Froth content, and the transcript stores that semantic content.</p>
    </subsection>
    <subsection title="4. Persist resolved config and provenance explicitly" id="4-persist-resolved-config-and-provenance-explicitly">
      <p>Today the agent runtime resolves provider/model/system/tools in memory and then mostly forgets the exact resolved shape.</p>
      <p>That should be persisted at cycle start. At minimum:</p>
      <list type="bullet">
        <item><p>resolved provider/module</p></item>
        <item><p>resolved model</p></item>
        <item><p>resolved tool specs</p></item>
        <item><p>thinking / effort settings</p></item>
        <item><p>app-level context identifiers</p></item>
        <item><p>bot/chat linkage where relevant</p></item>
      </list>
      <p>This is important for:</p>
      <list type="bullet">
        <item><p>reproducibility</p></item>
        <item><p>debugging</p></item>
        <item><p>cost analysis</p></item>
        <item><p>future replay or resume</p></item>
      </list>
      <p>The stored form does not need to inline huge prompts or blobs. Hashes plus blob refs are enough if the underlying content is retrievable.</p>
    </subsection>
    <subsection title="5. Add out-of-line blob storage for raw payloads and media" id="5-add-out-of-line-blob-storage-for-raw-payloads-and-media">
      <p>Raw vendor requests, raw vendor responses, SSE transcripts, and media blobs should not live inline in the hot path tables by default.</p>
      <p>The right pattern is:</p>
      <list type="bullet">
        <item><p>Postgres stores metadata, previews, hashes, and refs</p></item>
        <item><p>object storage stores the heavy payload</p></item>
      </list>
      <p>Examples of things that should become refs instead of inline JSONB:</p>
      <list type="bullet">
        <item><p>base64 image outputs</p></item>
        <item><p>uploaded PDFs</p></item>
        <item><p>full raw API request/response bodies</p></item>
        <item><p>captured SSE transcripts for fixture/replay/debug use</p></item>
      </list>
      <p>This matters for both cost and performance. Multimodal support will otherwise turn normal operational tables into accidental file archives.</p>
    </subsection>
    <subsection title="6. Make telemetry and execution records joinable by design" id="6-make-telemetry-and-execution-records-joinable-by-design">
      <p>Froth already emits spans around:</p>
      <list type="bullet">
        <item><p>LLM requests</p></item>
        <item><p>HTTP requests</p></item>
        <item><p>SSE events</p></item>
        <item><p>agent cycles</p></item>
        <item><p>agent think phases</p></item>
        <item><p>LLM edit projection</p></item>
      </list>
      <p>That is good. The missing piece is that the persisted agent runtime records should reference those spans directly.</p>
      <p>The rule should be simple:</p>
      <list type="bullet">
        <item><p>every persisted cycle has a root span ID</p></item>
        <item><p>every persisted LLM call or tool invocation has a span ID</p></item>
        <item><p>every execution-log event may point at the span that emitted it</p></item>
      </list>
      <p>This lets the UI answer both semantic and operational questions:</p>
      <list type="bullet">
        <item><p>what did the model say?</p></item>
        <item><p>how long did it take?</p></item>
        <item><p>which provider call produced it?</p></item>
        <item><p>where did the error happen?</p></item>
      </list>
    </subsection>
    <subsection title="7. De-emphasize legacy inference-session snapshots" id="7-de-emphasize-legacy-inference-session-snapshots">
      <p>The long-term direction should be:</p>
      <list type="bullet">
        <item><p>normalized Telegram messages</p></item>
        <item><p>normalized agent cycles</p></item>
        <item><p>explicit cycle links</p></item>
        <item><p>derived summaries and views</p></item>
      </list>
      <p>not cumulative conversation snapshots as the primary runtime record.</p>
      <p><tt>telegram_cycle_links</tt> is already a step in this direction. Keep that idea. Continue moving app-specific linkage into thin relation tables around a stronger core agent runtime model.</p>
    </subsection>
    <subsection title="8. Make control outcomes explicit, not textual" id="8-make-control-outcomes-explicit-not-textual">
      <p>A recent runtime failure made this concrete.</p>
      <p>Charlie ran a normal cycle, sent a status message successfully, then called <tt>read_tool_transcript</tt>. The tool completed successfully. The cycle then stopped <tt>reason=normal</tt> without taking the next think step or sending a real answer.</p>
      <p>The proximate cause was not a provider failure and not a process crash. The transcript tool had returned text containing the string <tt>Yielding:</tt> inside a code snippet. The worker was using a substring check on tool-result text to decide whether a tool had asked the cycle to yield. A transcript containing that text therefore triggered the same control path as a real yield.</p>
      <p>That is exactly the kind of bug an execution spine should make harder to write.</p>
      <p>Cycle control outcomes such as:</p>
      <list type="bullet">
        <item><p><tt>yield</tt></p></item>
        <item><p><tt>waiting_on_subscription</tt></p></item>
        <item><p><tt>reply_sent</tt></p></item>
        <item><p><tt>assistant_stopped_without_reply</tt></p></item>
        <item><p><tt>tool_error</tt></p></item>
        <item><p><tt>cycle_error</tt></p></item>
      </list>
      <p>should be represented as explicit runtime facts, not inferred from rendered text.</p>
      <p>In practice that means:</p>
      <list type="bullet">
        <item><p>tool execution should return typed outcomes for control flow</p></item>
        <item><p><tt>agent_events</tt> should persist those outcomes as first-class kinds or structured payload fields</p></item>
        <item><p>cycle-stop reasons shown to users should come from structured state and event history, not transcript heuristics</p></item>
        <item><p>transcript rows should remain semantic content, not scheduler control</p></item>
      </list>
      <p>The recent false-positive <tt>Yielding:</tt> stop bug was fixed in code by making yield explicit in the worker runtime. The persistence model should follow the same rule.</p>
    </subsection>
  </section>
  <section title="Schema Sketch" id="schema-sketch">
    <p>One plausible near-term shape:</p>
    <code>agent_cycles
  id
  status
  provider
  model
  root_span_id
  parent_span_id
  config_json
  usage_json
  cost_usd
  error
  started_at
  finished_at
  inserted_at

agent_events
  id
  cycle_id
  seq
  kind
  span_id
  parent_span_id
  message_id
  tool_use_id
  data_json
  blob_ref
  inserted_at

agent_messages
  id
  parent_id
  role
  content_json
  metadata_json
  inserted_at

telegram_cycle_links
  cycle_id
  bot_id
  chat_id
  reply_to
  legacy_inference_session_id
</code>
    <p>This is intentionally conservative. It evolves the tables Froth already has instead of requiring a whole new runtime subsystem at once.</p>
  </section>
  <section title="Migration Plan" id="migration-plan">
    <p>Phase 1: Add <tt>status</tt>, resolved config, usage, error, and span fields</p>
    <code>     to <tt>agent_cycles</tt>.
</code>
    <p>Phase 2: Expand <tt>agent_events</tt> into a typed execution log while</p>
    <code>     keeping the existing head-tracking semantics working during
     migration.
</code>
    <p>Phase 3: Persist LLM-call and tool-invocation boundaries as events</p>
    <code>     from <tt>Agent.Worker</tt>, including explicit control outcomes such
     as <tt>yield</tt>, <tt>reply_sent</tt>, and <tt>assistant_stopped_without_reply</tt>.
</code>
    <p>Phase 4: Introduce blob refs for large payloads and media, with</p>
    <code>     object-store-backed storage for the heavy data.
</code>
    <p>Phase 5: Update the cycle UI and related views to read cycle/event</p>
    <code>     metadata directly instead of reconstructing everything from
     transcript scraping.
</code>
    <p>Phase 6: Reduce dependence on legacy inference-session snapshots and</p>
    <code>     keep them only as migration/backfill compatibility.
</code>
  </section>
  <section title="Implementation Status" id="implementation-status">
    <p>As of 2026-03-24, the execution spine described here is implemented in Froth's runtime and persistence layers.</p>
    <p><tt>agent_cycles</tt> now stores run status, resolved configuration, usage, cost, errors, span linkage, and blob references; worker execution persists durable cycle, LLM, and tool records that the UI and tests can inspect directly.</p>
    <p>RFC-0008 later changed the exact event-log table shape by unifying execution events into <tt>events</tt>. That supersedes the specific <tt>agent_events</tt> schema proposed here, but not the architectural outcome.</p>
  </section>
  <section title="Consequences" id="consequences">
    <subsection title="Benefits" id="benefits">
      <list type="bullet">
        <item><p>Froth gets a durable model of execution, not just transcripts.</p></item>
        <item><p>Multimodal content stops threatening the hot operational tables.</p></item>
        <item><p>Debugging gets easier because config, spans, tool calls, and LLM turns can be queried directly.</p></item>
        <item><p>Resume/retry/cancel becomes architecturally possible instead of bolted on later.</p></item>
        <item><p>App-specific consumers like Telegram can link into the runtime model without defining it.</p></item>
      </list>
    </subsection>
    <subsection title="Costs" id="costs">
      <list type="bullet">
        <item><p>More schema and migration work.</p></item>
        <item><p>More explicit separation between transcript rows and blob storage.</p></item>
        <item><p>Temporary duplication while old and new persistence paths coexist.</p></item>
        <item><p>More care needed around retention, compaction, and object-store GC.</p></item>
      </list>
    </subsection>
  </section>
  <section title="Recommendation" id="recommendation">
    <p>Do not spend the next phase only adding more provider features.</p>
    <p>The next phase should build the execution spine in this order:</p>
    <list type="numbered">
      <item><p>richer <tt>agent_cycles</tt></p></item>
      <item><p>typed <tt>agent_events</tt></p></item>
      <item><p>explicit LLM/tool operation records and control outcomes via events</p></item>
      <item><p>blob refs for large payloads</p></item>
      <item><p>UI and query paths that consume the new records directly</p></item>
    </list>
    <p>That gives RFC-0002 somewhere durable to land and gives RFC-0003 a runtime model worthy of parallel execution.</p>
  </section>

</rfc>
